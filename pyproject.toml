[project]
name = "commission_record"
version = "0.0.1"
description = "Commission Record App"
requires-python = ">=3.10"

dependencies = [
    "frappe"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
